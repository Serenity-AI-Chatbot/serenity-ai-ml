{
    "version": 2,
    "builds": [
      {
        "src": "Sentiment.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "Sentiment.py"
      }
    ],

    "buildCommand": "python -m spacy download en_core_web_sm && python -c \"import nltk; nltk.download('punkt')\" && pip install -r requirements.txt && python Sentiment.py"

}